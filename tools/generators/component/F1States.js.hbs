export const IDLE = 'idle';

export function states() {
  /**
   * IDLE
   */

  const idle = {
    container: {
      style: {
        translate: [0, 0, 0],
      },
    }
  };

  return {
    idle,
  };
}
